{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "artifacts/nuasa"
  ],
  "scripts": {
    "dev": "npm --workspace=artifacts/nuasa run dev",
    "build": "npm --workspace=artifacts/nuasa run build",
    "install:all": "npm install"
  },
  "devDependencies": {
    "prettier": "^3.8.3",
    "typescript": "~5.9.3"
  }
}
